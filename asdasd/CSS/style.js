import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles((theme) => ({
    input: {
        height: "30px",
        width: "100%",
        fontSize: "20px",
        borderRadius: "25px",
        padding: "15px"
    },
}));